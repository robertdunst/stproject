{
  "name": "stproject",
  "version": "1.0.0",
  "description": "Repository for the Unit Testing Assignment.",
  "type": "module",
 "scripts": {
  "coverage": "jest --coverage || true && cat ./coverage/lcov.info | coveralls"
},
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-extended": "^7.0.0",
    "jest-chain": "^1.1.6",
    "coveralls": "^3.1.1",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0"
  }
}