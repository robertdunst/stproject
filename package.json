{
  "name": "stproject",
  "version": "1.0.0",
  "description": "Repository for the Unit Testing Assignment.",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "coverage": "c8 --reporter=lcov --reporter=text npm test",
    "coverage:ci": "c8 --reporter=lcov npm test && cat coverage/lcov.info | npx coveralls"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-extended": "^3.2.0",
    "jest-chain": "^1.0.3",
    "c8": "^7.12.0",
    "coveralls": "^3.1.1"
  }
}
