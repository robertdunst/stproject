{
  "name": "stproject",
  "version": "1.0.0",
  "description": "Repository for the Unit Testing Assignment.",
  "type": "module",
 "scripts": {
  "test": "jest --runInBand",
  "test:watch": "jest --watch",
  "coverage:ci": "c8 --reporter=lcov --all jest --runInBand || true"
},
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-extended": "^7.0.0",
    "jest-chain": "^1.1.6",
    "c8": "^7.12.0",
    "coveralls": "^3.1.1",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0"
  }
}
